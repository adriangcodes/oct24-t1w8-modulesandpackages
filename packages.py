# Packages

# python3 -m venv venv - in terminal - creates a hidden virtual environment folder venv to house package control
# ensure interpreter path is set to the local version within venv folder

# pip freeze - shows packages
# pip freeze > requirements.txt - creates text file with requirements